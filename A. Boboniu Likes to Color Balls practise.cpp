#include <bits/stdc++.h>
using namespace std;
int main()
{
    int t;
    cin >> t;
    while(t--)
    {
        long long a[4], minn=a[0];
        for(int i=0; i<4; i++)
        {
            cin >> a[i];
            if(a[i]<minn)
            {
                minn=a[i];
            }
        }
        if(a[0]==0 && a[1]==0 && a[2]==0 && a[3]==0)
        {
            y=1;
        }
        else if(a)
        for(int i=0; i<minn; i++)
        {

        }
    }
}
